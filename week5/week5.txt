✅ฝึก functional programming + pipeline ของข้อมูล
ใช้ map → filter → reduce แบบ chain (ต่อกัน)

✅ใช้ตอนที่ต้องสร้าง array เตรียมข้อมูล
ใช้ Array(length) เพื่อสร้าง array ตามความยาวที่กำหนด
.fill(value) ใส่ค่าซ้ำในทุก element ของ array


เช็ค null/undefined ก่อน → กัน error
ได้ความเข้าใจเรื่อง word frequency counting (นับคำซ้ำ) + object manipulation
.toLowerCase().split(" ") แปลงข้อความเป็นคำเล็กทั้งหมด
ใช้ object (freq) เก็บจำนวนครั้งที่คำปรากฏ

👉ใช้ทำ data cleaning
filter(Boolean) = ตัดค่าที่เป็น falsy ออก (เช่น false, 0, '', null, undefined, NaN)
ได้ array ที่เหลือแต่ค่าจริงๆ (truthy values)

การใช้ Higher-Order Functions (map, filter, reduce)
การ clean ข้อมูล (filter falsy, remove duplicate)
การ ประมวลผลข้อความ (split, lowercase, count)
การใช้ Object / Set เพื่อแก้ปัญหา